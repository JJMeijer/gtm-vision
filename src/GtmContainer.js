import React from 'react';
import PropTypes from 'prop-types';

class GtmContainer extends React.Component {
  constructor(props) {
    super(props);
  }
}

GtmContainer.propTypes = {
  container: PropTypes.object.isRequired,
};

export default GtmContainer;
